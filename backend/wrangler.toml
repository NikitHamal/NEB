name = "nebians-backend"
main = "src/index.ts"
compatibility_date = "2024-05-24"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "nebians-db"
database_id = "local-nebians-db" # Overridden in production, keeps wrangler local-dev running smoothly
migrations_dir = "./migrations"
