NEBians landing revision — changed files only

Modified:
- web/templates/base.html
  Adds a body_class template block so the floating-to-full-width topbar behavior is scoped to the home page only.
- web/templates/web/home.html
  Uses the revised compact hero, removes the previous feature strip, restores the original home sections, and adds the scroll-state topbar handler.
- web/static/web/css/pages/web-home.css
  Imports the two home landing style modules before the original home-page styles.

Added:
- web/templates/web/_home_hero.html
  Community-focused hero with real resource and forum data; no dashboard, fake metrics, eyebrow, or floating labels.
- web/static/web/css/pages/home/landing.css
  Compact hero, live community preview, restrained elevation, and home-only compact topbar.
- web/static/web/css/pages/home/landing-responsive.css
  Dark mode, mobile layout, and reduced-motion handling.
- LANDING_REVISION_NOTES.md
  Revision summary and deployment scope.

Unchanged:
- Backend models, migrations, views, APIs, authentication, and data behavior.
- Library, forum, news, profile, study lab, admin, and other page UI/UX.
- Android app package.
