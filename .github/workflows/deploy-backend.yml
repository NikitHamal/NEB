name: Deploy Backend to cPanel (Disabled)

# Disabled completely — backend deployments are handled manually / by agent via deploy.ps1
# to ensure local .env configuration, real-time logging, and verification checks.
on:
  workflow_dispatch:

jobs:
  disabled_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "GitHub CD deployment is disabled. Use local deploy.ps1 for verified deployments."
